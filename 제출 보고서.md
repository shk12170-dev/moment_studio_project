# 과제 5 — 대화가 끊겨도 이어지는 프로젝트 (제출 및 남길 기록)

---

## 1. 초기 조건 및 고정 기준

### A. 고정 검사 10개 및 기준
- **공동 적용 상한 설정**:
  - 시간 상한: AI당 최대 30분
  - 요청/호출 수 상한: AI당 최대 10회

| 검사 ID | 검사 이름 | 입력값 (Input) | 관찰 가능한 기대값 (Expected) |
| :--- | :--- | :--- | :--- |
| **T05-UT01** | 정상 JSON 로드 | 정상 규격의 JSON 파일 업로드 | `state` 정상 반영 및 성공 토스트 메시지 출력 |
| **T05-UT02** | 문법 오류 JSON 처리 | `{ ratio: '1:1', text: missingQuotes }` | 문법 오류 경고 토스트(`toast-error`) 출력 |
| **T05-UT03** | 필수 키 누락 JSON 처리 | `{"ratio":"1:1"}` | 필수 키 누락 경고 토스트 출력 |
| **T05-UT04** | 극단적/NaN 입력 보정 | `posX: NaN, fontSize: -9999` | `posX: 50, fontSize: 5`로 자동 보정 |
| **T05-UT05** | 회전 각도 범위 제한 | `rotation: 9999` | `rotation: 360`으로 상한 보정 |
| **T05-UT06** | 미지원 파일 업로드 거부 | `.txt` 확장자 파일 선택 | 업로드 거부 경고 토스트 출력 및 기존 상태 유지 |
| **T05-UT07** | 메타데이터 제거 다운로드 | 다운로드 버튼 클릭 | `toDataURL('image/png')` 기반 메타데이터 제거된 PNG 생성 |
| **T05-UT08** | LocalStorage 템플릿 동기화 | 템플릿 저장 버튼 클릭 | `<select>` 드롭다운 옵션 즉시 반영 및 동기화 |
| **T05-UT09** | 기본 프리셋 3종 생성 | 프리셋 생성 버튼 클릭 | 3개 프리셋 템플릿 생성 및 저장 |
| **T05-UT10** | DOM Null Check 방어 | 존재하지 않는 ID 요소 이벤트 접근 | 스크립트 에러 없이 무시/통과 (Optional Chaining) |

### B. 최초 요청 및 시작 정보
- **최초 요청 문구**: "app.js 파일의 JSON 불러오기/내보내기 예외 처리와 템플릿 동기화 로직을 보완하고 10개 테스트를 통과시켜줘."
- **시작 소스 주소**: `app.js` (버전 Hash / 고정 소스 상태)
- **AI A 시작 시각**: 2026-08-29 19:30 KST

---

## 2. AI A 작업 기록 및 중단 시점

### A. AI A 멈춘 소스 및 검사 결과
- **멈춘 시점 소스 버전 ID**: `commit-a1b2c3d4` (AI A 작업 완료 버전)
- **고정 검사 10개 결과**: 10 / 10 PASS (전체 통과)

### B. AI A 실제 사용량
- **실제 소요 시간**: 12분 (상한 30분 이내)
- **실제 요청/호출 수**: 3회 (상한 10회 이내)

---

## 3. 인수인계 문서

> 본 문서는 AI B(새 세션)에게 대화 전문 없이 제공되는 인수인계서입니다.

1. **목표 (Goal)**: JSON 불러오기/내보내기 예외 처리, 데이터 Sanitization, 템플릿 동기화 및 메타데이터 제거 다운로드 구현 완료.
2. **현재 상태 (Current Status)**: `app.js` 방어 로직 구현 완료 및 10개 고정 검사 시나리오 모두 PASS.
3. **실행 명령 (Execution Command)**: 
   - `npx live-server` 실행 후 `index.html` 접속.
   - 시크릿 창에서 F12 콘솔 열고 테스트 진행.
4. **통과 검사 (Passed Tests)**: T05-UT01 ~ T05-UT10 (10개 항목 전체 성공)
5. **남은 문제 (Remaining Issues)**: 없음. (AI A 할당 작업 완료)
6. **다음 행동 (Next Action for AI B)**: AI B는 인수인계받은 소스코드와 10개 고정 검사를 동일 환경에서 재현하여 동일성 및 완성도 검증.
7. **건드리지 말 것 / 금지 범위 (Do Not Touch)**:
   - `validateAndSanitizeInputs()` 내부의 NaN/음수 보정 로직
   - `REQUIRED_KEYS` 필수 키 검증 배열 및 Canvas PNG DataURL 추출 로직

---

## 4. AI A -> AI B 인수인계 및 동일성 확인

- **AI B 제공 자원**: 저장소 소스코드 (`app.js`) + 인수인계 문서 (대화 전문 미제공)
- **인수인계 문서 동일성 검증**:
  - AI A 남긴 인수인계 내용과 AI B 수령 내용 동일 여부: **동일함 (100% 일치)**
  - 인수인계 누락 사항: **없음 (0건)**
- **AI B 최종 검사 결과**: 10 / 10 PASS
- **AI B 실제 사용량**: 소요 시간 8분 / 호출 수 2회 (상한 이내)
- **전체 실행 순서 기록**: `A 시작` → `A 작업/종료` → `A 인수인계` → `B 시작` → `B 검증/완료`

---

## 5. 가린 비교표 및 제출 서식

### A. 블라인드 비교표 (가린 비교표)
| 구분 | 작업 시간 | 요청/호출 수 | 고정 검사 오류 회차 수 | 검사 통과 수 |
| :--- | :--- | :--- | :--- | :--- |
| **모델 A (AI A)** | 12분 | 3회 | 0회 | 10 / 10 |
| **모델 B (AI B)** | 8분 | 2회 | 0회 | 10 / 10 |

### B. 다음 작업 도구 선택 기준 (한 문장 정의)
> *"초기 단독 개발 및 복잡한 예외 처리 로직 구현에는 context 파악이 강한 AI A를 우선 배치하고, 작성된 문서 기반의 독립 검증 및 리팩토링 단계에는 가볍고 속도가 빠른 AI B를 배치한다."*
> 
